'use client';
import Link from 'next/link';import {ArrowRight,Building2,Eye,Mail,PauseCircle,PlayCircle,ShieldCheck} from 'lucide-react';import {useParams} from 'next/navigation';import {useState} from 'react';import {companies,PrivacyNotice,Shell,StatusBadge} from '../../_components';
export default function Detail(){const{id}=useParams<{tenantId:string}>();const company=companies.find(c=>c.id===id)||companies[0];const[status,setStatus]=useState(company.status);const[confirm,setConfirm]=useState(false);return <Shell title="تفاصيل الشركة"><Link href="/super-admin/tenants" className="mb-5 inline-flex items-center gap-2 text-sm font-bold text-[var(--sa-brand)]"><ArrowRight size={16}/>العودة إلى الشركات</Link><div className="mb-7 flex flex-col justify-between gap-4 md:flex-row md:items-end"><div className="flex items-center gap-4"><div className="grid h-14 w-14 place-items-center rounded-2xl bg-blue-50 text-[var(--sa-brand)]"><Building2/></div><div><div className="flex flex-wrap items-center gap-3"><h2 className="text-3xl font-black">{company.name}</h2><StatusBadge status={status}/></div><small dir="ltr" className="block text-left text-slate-400">{company.id} · {company.email}</small></div></div><div className="flex flex-wrap gap-2"><Link href={`/super-admin/tenants/${company.id}/support-view`} className="inline-flex items-center gap-2 rounded-xl border border-slate-200 bg-white px-4 py-3 text-sm font-bold"><Eye size={16}/>وضع الدعم الآمن</Link><button onClick={()=>setConfirm(true)} className={`inline-flex items-center gap-2 rounded-xl px-4 py-3 text-sm font-bold ${status==='نشطة'?'bg-orange-50 text-orange-700':'bg-emerald-600 text-white'}`}>{status==='نشطة'?<PauseCircle size={16}/>:<PlayCircle size={16}/>} {status==='نشطة'?'تعليق الشركة':'إعادة التفعيل'}</button></div></div><div className="grid gap-5 md:grid-cols-2"><section className="rounded-2xl border border-[var(--sa-border)] bg-white p-6 shadow-sm"><h3 className="mb-5 text-lg font-black">ملخص الشركة</h3><div className="grid gap-5 sm:grid-cols-2"><div><small className="text-slate-400">الباقة</small><b className="mt-1 block">{company.plan}</b></div><div><small className="text-slate-400">تاريخ الانضمام</small><b className="mt-1 block">{company.onboarded}</b></div><div><small className="text-slate-400">آخر نشاط منصّي</small><b className="mt-1 block">{company.activity}</b></div><div><small className="text-slate-400">المسؤول</small><b className="mt-1 flex items-center gap-2"><Mail size={15} className="text-slate-400"/>{company.admin}</b></div></div></section><section className="rounded-2xl border border-[var(--sa-border)] bg-white p-6 shadow-sm"><h3 className="mb-5 text-lg font-black">النشاط المنصّي الأخير</h3>{['تم تسجيل دخول مدير الشركة','تم تحديث بيانات الباقة','تمت مزامنة حالة الشركة'].map((event,i)=><div key={event} className="border-b border-slate-100 py-3 last:border-0"><b className="block text-sm">{event}</b><small className="text-xs text-slate-400">{i===0?'قبل ١٥ دقيقة':'منذ يوم'}</small></div>)}</section></div><PrivacyNotice/>{confirm&&<div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/40 p-5"><div className="w-full max-w-md rounded-2xl bg-white p-6 shadow-2xl"><h3 className="text-xl font-black">تأكيد {status==='نشطة'?'تعليق':'إعادة تفعيل'} الشركة</h3><p className="mt-3 text-sm leading-7 text-slate-500">{status==='نشطة'?'سيؤدي هذا الإجراء إلى فقدان الشركة إمكانية الوصول إلى المنصة فوراً.':'ستتم إعادة إتاحة وصول الشركة إلى المنصة.'}</p><div className="mt-6 flex justify-end gap-3"><button onClick={()=>setConfirm(false)} className="rounded-xl px-4 py-2 text-sm font-bold text-slate-500">إلغاء</button><button onClick={()=>{setStatus(status==='نشطة'?'معلقة':'نشطة');setConfirm(false)}} className="rounded-xl bg-[var(--sa-brand)] px-5 py-2 text-sm font-bold text-white">تأكيد</button></div></div></div>}</Shell>}
