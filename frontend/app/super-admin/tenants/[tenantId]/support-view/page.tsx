'use client';
import Link from 'next/link';import {ArrowRight,Building2,EyeOff,ShieldCheck} from 'lucide-react';import {useParams} from 'next/navigation';import {companies,Shell} from '../../../_components';
export default function SupportView(){const{id}=useParams<{tenantId:string}>();const c=companies.find(x=>x.id===id)||companies[0];return <Shell title="وضع الدعم"><div className="mb-6 flex items-center justify-between"><Link href={`/super-admin/tenants/${c.id}`} className="inline-flex items-center gap-2 text-sm font-bold text-[var(--sa-brand)]"><ArrowRight size={16}/>إنهاء وضع الدعم</Link><span className="rounded-full bg-amber-100 px-3 py-1.5 text-xs font-black text-amber-800">قراءة فقط</span></div><div className="mb-6 rounded-2xl border border-amber-200 bg-amber-50 p-5 text-amber-950"><div className="flex items-start gap-3"><ShieldCheck className="mt-1 shrink-0 text-amber-700"/><div><b>وضع الدعم — عرض {c.name} كقراءة فقط</b><p className="mt-1 text-sm leading-7 text-amber-800">تظهر هنا معلومات الشركة والنشاط المنصّي فقط. لا يمكن الوصول إلى المستخدمين أو المركبات أو العمليات الخاصة بالشركة.</p></div></div></div><section className="rounded-2xl border border-[var(--sa-border)] bg-white p-7 shadow-sm"><div className="flex items-center gap-4 border-b border-slate-100 pb-6"><div className="grid h-14 w-14 place-items-center rounded-2xl bg-blue-50 text-[var(--sa-brand)]"><Building2/></div><div><h2 className="text-2xl font-black">{c.name}</h2><small className="text-slate-400">عرض دعم آمن · {c.id}</small></div></div><div className="grid gap-6 py-7 sm:grid-cols-2"><div><small className="text-slate-400">حالة الشركة</small><b className="mt-1 block text-emerald-700">{c.status}</b></div><div><small className="text-slate-400">الباقة</small><b className="mt-1 block">{c.plan}</b></div><div><small className="text-slate-400">المسؤول</small><b className="mt-1 block">{c.admin}</b></div><div><small className="text-slate-400">البريد الإلكتروني</small><b dir="ltr" className="mt-1 block text-left">{c.email}</b></div><div><small className="text-slate-400">تاريخ الانضمام</small><b className="mt-1 block">{c.onboarded}</b></div><div><small className="text-slate-400">آخر نشاط منصّي</small><b className="mt-1 block">{c.activity}</b></div></div><div className="rounded-2xl bg-slate-50 p-5"><div className="mb-3 flex items-center gap-2 font-black"><EyeOff size={17} className="text-slate-400"/>حدود الخصوصية</div><p className="text-sm leading-7 text-slate-500">بيانات المستخدمين والمركبات والمواقع والمسارات والسجلات التشغيلية غير متاحة في وضع الدعم.</p></div></section></Shell>}
