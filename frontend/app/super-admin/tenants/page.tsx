'use client';
import Link from 'next/link';import {Plus,Search,Eye,ArrowRight} from 'lucide-react';import {useMemo,useState} from 'react';import {companies,PrivacyNotice,Shell,StatusBadge} from '../_components';
export default function Tenants(){const[q,setQ]=useState('');const[status,setStatus]=useState('الكل');const rows=useMemo(()=>companies.filter(c=>(status==='الكل'||c.status===status)&&`${c.name} ${c.email}`.includes(q)),[q,status]);return <Shell title="الشركات"><div className="mb-8 flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><Link href="/super-admin" className="mb-3 flex items-center gap-1 text-xs font-bold text-[var(--sa-brand)]"><ArrowRight size={14}/>لوحة التحكم</Link><h2 className="text-3xl font-black">الشركات</h2><p className="mt-1 text-sm text-[var(--sa-text-secondary)]">إدارة بيانات الشركات ومتابعة نشاط المنصة فقط.</p></div><Link href="/super-admin/tenants/new" className="inline-flex items-center gap-2 rounded-xl bg-[var(--sa-brand)] px-5 py-3 text-sm font-bold text-white"><Plus size={17}/>إضافة شركة</Link></div><div className="rounded-2xl border border-[var(--sa-border)] bg-white shadow-sm"><div className="flex flex-col gap-3 border-b border-slate-100 p-5 md:flex-row"><div className="relative flex-1"><Search className="absolute right-3 top-2.5 text-slate-400" size={17}/><input value={q} onChange={e=>setQ(e.target.value)} className="w-full rounded-xl border border-slate-200 bg-slate-50 py-2.5 pr-10 text-sm outline-none focus:border-blue-500" placeholder="البحث باسم الشركة أو البريد..."/></div><select value={status} onChange={e=>setStatus(e.target.value)} className="rounded-xl border border-slate-200 px-4 py-2 text-sm font-bold"><option>الكل</option><option>نشطة</option><option>معلقة</option></select></div><div className="overflow-x-auto"><table className="w-full min-w-[800px] text-right text-sm"><thead className="bg-slate-50 text-xs text-slate-400"><tr><th className="px-5 py-4">الشركة</th><th className="px-5 py-4">الحالة</th><th className="px-5 py-4">الباقة</th><th className="px-5 py-4">المسؤول</th><th className="px-5 py-4">آخر نشاط</th><th className="px-5 py-4">تاريخ الانضمام</th><th/></tr></thead><tbody className="divide-y divide-slate-100">{rows.map(c=><tr key={c.id} className="hover:bg-slate-50"><td className="px-5 py-4"><Link href={`/super-admin/tenants/${c.id}`} className="font-bold">{c.name}<small dir="ltr" className="block text-left text-xs font-normal text-slate-400">{c.email}</small></Link></td><td className="px-5 py-4"><StatusBadge status={c.status}/></td><td className="px-5 py-4 text-xs font-bold">{c.plan}</td><td className="px-5 py-4 text-xs">{c.admin}</td><td className="px-5 py-4 text-xs text-slate-500">{c.activity}</td><td className="px-5 py-4 text-xs text-slate-500">{c.onboarded}</td><td className="px-5 py-4"><Link href={`/super-admin/tenants/${c.id}/support-view`} title="عرض الدعم الآمن" className="text-slate-400 hover:text-[var(--sa-brand)]"><Eye size={17}/></Link></td></tr>)}</tbody></table>{!rows.length&&<div className="p-12 text-center text-sm text-slate-400">لا توجد نتائج مطابقة.</div>}</div></div><PrivacyNotice/></Shell>}
